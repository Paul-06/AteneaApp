<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cells="clr-namespace:AteneaApp.Cells"
             xmlns:viewmodels="clr-namespace:AteneaApp.ViewModels"
             x:DataType="viewmodels:ServiceViewModel"
             x:Class="AteneaApp.Views.ServicesPage">
    <ContentPage.Content>
        <StackLayout Style="{StaticResource Container}">
            <Label Text="{Binding Titulo}" Style="{StaticResource Title}" HorizontalOptions="Center"/>
            <ListView ItemsSource="{Binding Services}" HasUnevenRows="True" SelectionMode="None" SeparatorVisibility="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell IsEnabled="False">
                            <cells:ServiceItemCard/>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>